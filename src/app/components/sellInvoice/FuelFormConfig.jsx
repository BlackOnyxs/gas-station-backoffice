import React from 'react';
import { Form, Select } from 'antd';

import { validOctane, validFuel } from '../../../data/menus';
import { useBuyInvoiceStore } from '../../../hooks';

export const FuelFormConfig = () => {
  const { products } = useFuelStore();
  return (
    <>
        
    </>
  )
}
